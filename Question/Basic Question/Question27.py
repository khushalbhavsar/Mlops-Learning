# 27. Create a tuple and print its length.

my_tuple = (1, 2, 3, 4, 5) # This is a tuple containing 5 elements
tuple_length = len(my_tuple) # The `len()` function returns the number of items in the tuple 